Read `GPT-HUMAN-PLAN.md`, `CLAUDE.md`, and `WORKFLOW.md` first, in that order. Treat them as binding project context. Use them to distinguish the locked target architecture from the current implementation state. Do not redesign the wallet model, session model, deployment shape, security model, risk model, observability model, or data map. First map the current codebase to the locked plan, identify the real next unfinished step, and continue from there.
